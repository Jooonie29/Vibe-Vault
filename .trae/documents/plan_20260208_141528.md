I will fix the dark mode issues in `NotificationsPanel.tsx` and `CommandPalette.tsx` by adding `dark:` Tailwind classes to the hardcoded light mode styles.

### **1. Update `NotificationsPanel.tsx`**
- **Helper Function (`getNotificationColor`)**: Update color strings to include dark mode variants (e.g., `dark:bg-blue-900/30 dark:text-blue-400`).
- **Main Container**: Update background to `dark:bg-gray-900` and border to `dark:border-gray-800`.
- **Header**: Update background to `dark:bg-gray-800/50`, border to `dark:border-gray-800`, and text colors.
- **Empty State**: Update icon background and text colors for dark mode.
- **List Items**:
  - Add `dark:hover:bg-gray-800/50` for hover states.
  - Update unread background to `dark:bg-violet-900/10`.
  - Update text colors (`text-gray-900` → `dark:text-gray-100`, etc.).
- **Invite Actions**: Ensure buttons look good in dark mode (secondary button specifically).

### **2. Update `CommandPalette.tsx`**
- **Modal Container**: Update background to `dark:bg-gray-900`.
- **Search Input**:
  - Update border to `dark:border-gray-800`.
  - Update input text to `dark:text-gray-100` and placeholder to `dark:placeholder-gray-500`.
  - Update ESC key styling to `dark:bg-gray-800 dark:text-gray-400`.
- **Command List Items**:
  - Update default text to `dark:text-gray-300` and hover background to `dark:hover:bg-gray-800`.
  - Update selected state to `dark:bg-violet-900/20 dark:text-violet-300`.
  - Update icon backgrounds (`dark:bg-gray-800` for default, `dark:bg-violet-900/30` for selected).
- **Footer**:
  - Update background to `dark:bg-gray-800/50` and border to `dark:border-gray-800`.
  - Update keyboard shortcuts to `dark:bg-gray-900 dark:border-gray-700`.

I will start by creating a todo list and then proceed with the edits.
