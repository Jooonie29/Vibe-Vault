I will implement the email-based user search and improved invitation notification system as follows:

### 1. Database Schema & Backend Updates
*   **Update `convex/schema.ts`**: Add an `email` field to the `profiles` table and create an index `by_email` to enable searching.
*   **Update `convex/profiles.ts`**:
    *   Update `updateProfile` (or create a new `syncUser` mutation) to store the user's email in the `profiles` table.
    *   Create a `searchUsers` query that allows searching for users by email (prefix match) to support the autocomplete feature.
*   **Update `convex/teams.ts`**:
    *   Modify `inviteMember` to check if a user with the invited email already exists in the database.
    *   If the user exists, create a notification specifically for them (instead of just notifying existing team members).
    *   Create a `respondToInvite` mutation that allows a user to **Accept** or **Decline** an invitation directly.

### 2. Frontend Implementation
*   **User Synchronization**: Create a mechanism (likely a `UserSync` component) to ensure the logged-in user's email is synced to the Convex database so they can be found by others.
*   **Invite Modal (`InviteModal.tsx`)**:
    *   Implement an autocomplete dropdown in the email input field.
    *   Use the `searchUsers` query to fetch matching users as you type.
    *   Allow selecting a user to auto-fill the email.
*   **Notifications Panel (`NotificationsPanel.tsx`)**:
    *   Update the notification item design for "Invite" type notifications.
    *   Add **Accept** and **Decline** buttons to these notifications.
    *   Connect these buttons to the `respondToInvite` mutation to handle the user's choice.
