I will modify `src/components/dashboard/Dashboard.tsx` to conditionally render the chat button for team members.
The chat button should only be visible if the `member.userId` is NOT equal to the current user's ID (`storeUser.id`).

1.  **Read `storeUser`**: `const { user: storeUser } = useAuthStore();` is already present in the component.
2.  **Locate the loop**: The loop iterates over `teamMembers`.
3.  **Add Condition**: Inside the loop, before rendering the chat button, check if `member.userId !== storeUser?.id`.

```tsx
                  {teamMembers?.slice(0, 4).map((member, i) => (
                    <div key={member.id} className="flex items-center justify-between py-2">
                      {/* ... user info ... */}
                      
                      {/* Chat Icon Button - Only show if not the current user */}
                      {member.userId !== storeUser?.id && (
                        <button
                          onClick={() => openFloatingChatWithUser(member.userId)}
                          className="p-2 rounded-lg text-muted-foreground hover:text-violet-600 hover:bg-violet-50 dark:hover:bg-violet-900/20 transition-colors"
                        >
                          <MessageSquare className="w-4 h-4" />
                        </button>
                      )}
                    </div>
                  ))}
```

I will verify the variable names: `member.userId` and `storeUser.id` are correct based on the code reading.

I'll start by creating a todo list and then proceed with the edits.
